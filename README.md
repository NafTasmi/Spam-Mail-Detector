# Spam-Mail-Detector
A Python spam detector with Tkinter GUI that analyzes emails using rules and ML. It scans for suspicious keywords, domains, and patterns, applies a trained Naive Bayes model, and provides risk scores with visual statistics. Features include single/batch processing, real-time charts for effective email threat identification.
